# Legacy Deployment

This directory preserves the original competition-style Docker deployment for
VOCAEND.

The public archive no longer treats Docker as the primary way to run the
project. The Dockerfile remains here so the old `socat` service shape,
`GZCTF_FLAG` loading behavior, and `ping`-based final step can be inspected or
recreated if needed.
